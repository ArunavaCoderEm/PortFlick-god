version: '3.8'

services:
  backend:
    build: .
    container_name: portflick-backend
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    depends_on:
      - redis
    volumes:
      - .:/app
    restart: always

  redis:
    image: redis:alpine
    container_name: portflick-redis
    ports:
      - "${REDIS_PORT}:${REDIS_PORT}"
    restart: always
